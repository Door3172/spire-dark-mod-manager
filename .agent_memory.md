# Spire Dark Mod Manager - 專案記憶與知識庫 (Project Memory)

本檔案為 AI 助理的專案記憶庫。在新的對話或專案切換時，請提示 AI 讀取此檔案以快速同步背景上下文。

---

## 1. 專案基本資訊
* **專案名稱**：Spire Dark Mod Manager (殺戮尖塔 2 模組管理器)
* **本地工作區路徑**：`c:\Users\JA_User2\Documents\杀戮尖塔2整合V4.0\杀戮尖塔2整合V4.0`
* **GitHub 倉庫**：`https://github.com/Door3172/spire-dark-mod-manager`
* **分發網站 (Vercel)**：`https://spire-dark-mod-manager.vercel.app/`
* **統一資料源**：`web_portal/public/manifest.json` (包含管理器最新版本、下載連結、以及 161 個模組的清單)

---

## 2. 系統架構
* **前端 (Webview UI)**：
  - 基於 HTML、Vanilla CSS 和 JavaScript 設計的 Glassmorphic 介面。
  - 與 Python 後端以 Bottle 框架建立的 REST API 通訊 (Port 18690)。
* **後端 (Python Core)**：
  - 核心檔案：`mod_manager/main.py`。
  - 採用多線程運行 Bottle 服務 + `pywebview` 喚起原生無邊框視窗。
* **分發與自動更新**：
  - 管理器在啟動時會靜默請求 `manifest.json`。
  - 比對本地 `CURRENT_VERSION` 與線上的 `version`。
  - 若有新版，下載最新 EXE 至本地為 `.tmp`，生成後台腳本 `updater.bat`，關閉主程式並由批次檔替換原 EXE，最後自動重啟。

---

## 3. 關鍵 BUG 記錄與防坑指南

### 3.1 遊戲路徑與分類偵測 (繁簡相容)
* **問題**：本地硬碟上的資料夾名稱為簡體字「`必装前置`」，但代碼中誤用繁體字「`必裝前置`」進行 `os.path.exists` 檢查，導致工作區路徑判斷失效，回退至父目錄，漏掉所有整合包的模組掃描。
* **解決方案**：在 `main.py` 的路徑檢查中同時加入繁簡體判定：
  ```python
  if os.path.exists(os.path.join(EXE_DIR, "必装前置")) or os.path.exists(os.path.join(EXE_DIR, "必裝前置")):
      WORKSPACE_DIR = os.path.abspath(EXE_DIR)
  ```

### 3.2 停用模組自動被勾選啟用
* **問題**：`sync_local_mods_to_default()` 在每次啟動時都會掃描 `mods_disabled/` 下的實體資料夾並加入啟用名單。
* **解決方案**：已移除該函式中對 `mods_disabled/` 的掃描邏輯。停用資料夾下的模組應保持未勾選狀態，直到用戶手動在 UI 啟用。

### 3.3 PyInstaller 6.x 打包後運行報錯「Failed to load Python DLL」
* **問題**：GitHub Actions Runner 預設安裝最新 PyInstaller (6.x)。但自 PyInstaller 6.0 起，**移除了對 `win_private_assemblies` 的支援**。若不打包 MSVC 執行庫 DLL，打包出的 EXE 在沒有安裝對應 Python 環境的用戶電腦上會因為找不到 `python310.dll` 等相依庫而直接崩潰。
* **解決方案**：
  - **方案 A (最穩定)**：在 `.github/workflows/release.yml` 中將 PyInstaller 鎖定在 6.0 以下（`pip install "pyinstaller<6.0"`），並在 `main.spec` 中重新加入 `win_private_assemblies=True`，交由舊版 PyInstaller 自動收集。
  - **方案 B**：使用 PyInstaller 6.x，並在 `main.spec` 的 `binaries` 中手動填寫相依 DLL 的路徑。

---

## 4. 自動化建置工作流 (GitHub Actions)
* 觸發條件：推送以 `v*` 開頭的 Git Tag (例如 `v1.0.3`)。
* 工作流步驟 (`.github/workflows/release.yml`)：
  1. 檢出代碼，使用 Python `3.10.6`。
  2. 安裝依賴並使用正則表達式自動替換 `main.py` 中的 `CURRENT_VERSION`。
  3. 運行 PyInstaller 編譯生成 EXE。
  4. 自動解析 `manifest.json`，更新下載網址與版本號，並以 `[skip ci]` 自動推回 `main` 分支。
  5. 創建 GitHub Release 並上傳最新的 `SpireDarkModManager.exe`。
